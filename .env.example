GROQ_API_KEY= #Add your Groq API key

MODEL_NAME=llama-3.1-8b-instant

EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Auth (choose one)
# - Recommended: set a strong local API key and use it in the frontend Connection panel.
API_SECRET_KEY=
# - Dev only: bypass auth checks (any bearer token accepted). Do not use in production.
DEBUG_SKIP_AUTH=false